¿Que afirmación es correcta en relación a la tecnología SSE?
3
- No permite en ningún caso utilizar los registros de propósito general.
- No permite operaciones de transferencia entre registros XMM y la memoria.
- No incorpora instrucciones de salto especiales.

Tras la siguiente instrucción SSE, si xmm0 = [10, 20, 30, 40]: \n ``` shufps xmm0, xmm0, 0x5A ``` \n ¿Cuál será el nuevo contenido de xmm0? Seleccione una:
1
- 20, 20, 30, 30
- 20, 30, 20, 30
- 10, 20, 20, 40

``` runspec --reportable --config=win32-x86-vc7.cfg -T base int ``` \n¿Qué cambio en la línea de comandos de arriba se debe hacer para seleccionar eI benchmark suite CFP2000? Seleccione una:
1
- Cambiar int por fp.
- Cambiar int por cfp.
- Cambiar base int por peak fp.

Indica el tipo de arquitectura que mejor se ajusta a la operación de multiplicación de un número por las componentes de un vector. Seleccione una:
2
- SISD
- SIMD
- MISD

En ensamblador, cuando hablamos de word, nos referimos a...
1
- Tipos de datos de 2 bytes de tamaño
- Un conjunto de ocho bits
- Un procesador de texto

¿En qué lenguajes estan escritas las aplicaciones que utiliza SPEC como benchmarks?
2
- C, C++ y C#
- C, C++ y Fortran
- Java, Python y C

¿Que opción es correcta como Instrucctón SSE para leer un dato de la memoria?
2
- movapd xmmO, [xmm1 + ebx]
- movapd xmm0, [esi + ebx]
- movapd ebx,  [xmm1 + ebx]

El ejemplo "inner product" contiene la función "sse3_inner" que utiliza la instrucción "addps". Esta instrucción realiza lo siguiente:
1
- Suma valores empaquetados en punto flotante de simple precisión
- Suma valores empaquetados enteros
- Suma valores empaquetados en un punto flotante de doble precisión

```int DataTransferOptimised(int* pidst, int* pisrc, unsigned long SizeInBytes)\n{\n\tunsigned long dwNumElements = SizeInBytes / sizeof(int);\n\tunsigned long dwNumPacks = dwNumElements / (128/(sizeof(int)*8));\n\n\t__asm\n\t{\n\t\tpusha;\n\tbegin:\n\t\tmov ecx, SizeInBytes;\n\t\tmov edi,pidst;\n\t\tmov esi, pisrc;\n\tbegina:\n\t\tcmp ecx,0;\n\t\tjz end;\n\tbody:\n\t\n\t\tmov ebx, SizeInBytes;\n\t\tsub ebx, ecx;\n\t\n\t\tmovdqa xmm1, [esi+ebx];\n\t\tmovdqa [edi+ebx], xmm1;\n\t\n\tbodya:\n\t\tsub ecx, 16;\n\t\tjmp begina;\n\tend:\n\t\tpopa;\n\t}\n\n\treturn 0;\n}\n\n#pragma warning(pop)\n``` \n La rutina data transfer optimizada realiza transferencias de datos entre memoria utilizando dos instrucciones "movdqa" que transfieren:
3
- Los 8 bytes ubicados a partir la direccion de memoria [edi+ebx] hasta la direccion de memoria [esi+ebx], utilizando xmm1 como registro intermedio
- Los 16 bytes ubicados a partir la direccion de memoria [edi+ebx] hasta la direccion de memoria [esi+ebx], utilizando xmm1 como registro intermedio
- Los 16 bytes ubicados a partir la direccion de memoria [esi+ebx] hasta la direccion de memoria [edi+ebx], utilizando xmm1 como registro intermedio

¿Que afirmación es incorrecta en relación a la tecnología SSE?
2
- SSE proporciona 8 nuevos registros de 128 bits
- SSE utiliza los registros de la Unidad en Punto Flotante
- SSE permite operar con datos empaquetados en punto flotante

```__asm { \n\t(...) \n\tbucle: \n\t\t... código ... \n\t\t... 1000 instrucciones más ... \n\tCERRAR bucle \n}``` \n En el código ensamblador x86 anterior ¿Qué instrucción deberá ser CERRAR?
2
- JMP, puesto que no se trata de una etiqueta corta (<128 posiciones)
- LOOP, puesto que se trata de un bucle
- CLOSE, puesto que CERRAR es pseudocódigo

¿Qué diferencia los conjuntos de pruebas CINT2000 de CFP2000?
2
- CINT contiene algoritmos de uso intensivo (INTensive); CFP son problemas matemáticos p-completos (full p).
- CINT son benchmarks intensivos con operaciones de enteros, CFP para coma flotante (floating point)
- Trabajan interrupciones constantes al sistema (CINT) y ejecuciones completas del programa, full program (CFP). La 'C' en ambos indica que se trata de problemas de menor intensidad que los conjuntos 'A' (AINT, AFP) o 'B' (BINT, BFP).

¿Qué parámetros contiene el archivo vcvars32.bat?
3
- Contiene variables de entorno para encontrar/habilitar el compilador desde la línea de comandos
- Contiene variables de entorno del sistema operativo, referentes al uso de multitarea por parte de la CPU.
- Contiene variables de entorno referentes a la elección de un subconjunto de pruebas SPEC.

```int vector[5] = {1,2,3,4,5}; \n__asm \n\t{ \n\t\tmov ecx, 2 \n\t\tmov eax, [vector+ecx*4+4] \n\t\tmov edx, [vector+ecx*4] \n\t\tmov [vector+ecx*4], eax \n\t\tmov [vector+ecx*4+4], edx \n\t}``` \n¿Cómo quedaría vector tras la ejecución de las instrucciones del código x86 anterior?
1
- vector[5] = {1,2,4,3,5};
- vector[5] = {1,3,2,4,5};
- vector[5] = {1,2,3,5,4};

```__asm \n{ \n\tmov eax, edx \n}``` \nAnte este código x86 embebido, ¿qué ocurrirá?
1
- Que el contenido de edx pasará a eax
- Un error de compilación al faltar un ";" al final de línea
- Que el contenido de eax pasará a edx

Según la taxonomía de Flynn, ¿en qué clasificación se incluirían las extensiones MMX/SSE?
1
- Single Instruction Multiple Data
- Multiple Instruction Multiple Data
- En cualquiera de ellas, MMX y SSE sirven para muchas cosas

¿Qué hace la instrucción SSE "cmpltpd"?
1
- Realiza el test de menor que de cada una de las dos quadruples palabras de 64 bits de los operandos.
- Realiza el test de menor o igual que de cada una de las dos quadruples palabras de 64 bits de los operandos.
- Realiza el test de menor que de cada uno de los bytes de los operandos.

¿Qué diferencia los conjuntos de pruebas CINT2000 de CFP2000?
1
- CINT son benchmarks intensivos con operaciones de enteros, CFP para coma flotante (floating point)
- Trabajan interrupciones constantes al sistema (CINT) y ejecuciones completas del programa, full program (CFP). La 'C' en ambos indica que se trata de problemas de menor intensidad que los conjuntos 'A' (AINT, AFP) o 'B' (BINT, BFP).
- CINT contiene algoritmos de uso intensivo (INTensive); CFP son problemas matemáticos p-completos (full p).

```constexpr int kElems = 40; \nint vec[kElems] = {1, 2, 3, (...), 39, 40}; \n_asm \n{ \n\tmov esi, vec \n\tmov ecx, kElems \n\tbucle: \n\t\t(... decrementa ecx, salta a fin si 0) \n\t\tmov eax, [esi] \n\t\tshl eax, 2 \n\t\tadd esi, 4 \n\t\t(... esi pasa a siguiente posición vector) \n\t\tloop bucle \n\tfin: \n}``` \nAnte este código x86 embebido, ¿qué ocurrirá?
3
- Los elementos del vector vec quedarán multiplicados por 2
- Los elementos del vector vec quedarán multiplicados por 4
- Los elementos del vector vec, serán desplazados todos a la izquierda una posición del vector (vec[i] = vec[i+1])

```int vector[5] = {1,2,3,4,5}; \n_asm \n\t{ \n\t\tmov ecx, 2 \n\t\tmov eax, [vector+ecx*4+4] \n\t\tmov edx, [vector+ecx*4] \n\t\tmov [vector+ecx*4], eax \n\t\tmov [vector+ecx*4+4], edx \n\t}``` \n¿Cómo quedaría vector tras la ejecución de las instrucciones del código x86 anterior?
2
- vector[5] = {1,3,2,4,5};
- vector[5] = {1,2,4,3,5};
- vector[5] = {1,2,3,5,4};

```int vector[20] = {2,30,45,2,7,4,8,1,4,5,7,9,5,4,0,6,7,6,1,3}; \n__asm \n\t{ \n\t\tmov edi, 19 \n\t\tmov edx, [vector+4*edi] \n\t}``` Ante este código x86 embebido, ¿qué valor será movido al registro EDX?
1
- 3
- Causará un error de memoria por intentar acceder más allá de la longitud del vector
- 2

```int vector[20] = {2,30,45,2,7,4,8,1,4,5,7,9,5,4,0,6,7,6,1,3}; \n__asm \n\t{ \n\t\tmov edi, 0 \n\t\tinc edi \n\t\t.... \n\t}``` ¿Qué instrucción añadiría al código x86 anterior para leer un elemento del vector?
1
- mov eax, [vector+4*edi]
- mov eax, [vector+edi]
- mov [vector+4*edi], eax

```int a,b; \n__asm \n{ \n\tmov a, b \n}``` \n Ante este código x86 embebido, ¿qué ocurrirá?
3
- Que el contenido de la posición de memoria a se copia en la posición de memoria b
- Que el contenido de la posición de memoria b se copia en la posición de memoria a
- Error de compilación: Tipo de operando incorrecto. mov no soporta dos referencias a memoria

¿Cual de las siguientes afirmaciones sobre SPEC es falsa?
1
- Todas las aplicaciones de CINT2000 y CFP2000 están escritas en C y C++
- SPEC CPU2000 contiene dos suits CINT2000 y CFP2000
- Los principales objetivos de SPEC son: crear un benchmark estándar para medir el rendimiento de computadores y controlar y publicar los resultados de estos tests

```int vector[20] = {2,30,45,2,7,4,8,1,4,5,7,9,5,4,0,6,7,6,1,3}; \n__asm \n\t{ \n\t\tmov edi, 0 \n\t\tadd edi, 1 \n\t\tmov eax, [vector+4*edi] \n\t}``` \nAnte este código x86 embebido, ¿qué valor será movido al registro EAX?
2
- 7
- 30
- 2

```_asm \n{ \n\tmov eax, [ebx] \n}``` \n Ante este código x86 embedido, ¿qué ocurrirá?
1
- Que el contenido de la posición de memoria indicada por ebx pasará a eax.
- Que el contenido de eax pasará a la posición de memoria indicada por ebx.
- Que el contenido de ebx pasará a eax

Si un bucle sobre un vector de 200 enteros necesita 200 iteraciones, ¿Cuántas tardará usando extensiones MMX/SSE?
2
- 100, ya que cada instrucción MMX/SSE sólo puede trabajar con precisión doble.
- 50, ya que cada instrucción MMX/SSE puede trabajar sobre 4 enteros a la vez.
- 200, ya que no varia, las instrucciones quedarán igual.

Ante la instrucción para recorrer vector usando edi como valor de iteración: ```mov eax, [vector + edi]``` ¿De qué tipo de datos es vector?
2
- Cualquier tipo de dato
- Un tipo de dato de 8 bits de tamaño, por ejemplo char
- Tendrfamos que ver la declaración de vector

¿Cómo obtenemos las características de la máquina donde ejecutamos el SPEC?
2
- El SPEC saca un informe del Sistema Operativo, pero debemos usar otros programas para obtener información del Hardware
- El propio SPEC saca un informe detallado del Hardware y del Sistema Operativo
- Debemos utilizar programas específicos para ello

```int a,b; \na=10; \nb=3; \n_asm \n{ \n\tmov eax, a \n\tmov ebx, b \n\tmov ecx, 0 \n\tbucle: \n\tcmp eax, ebx \n\t\tjs fin \n\t\tinc ecx sub eax, ebx \n\t\tjmp bucle \n\t\tfin : \n}``` \n¿Que afirmación es incorrecta?
1
- La instrucción "js" salta cuando el bit de signo de EFLAGS es 0
- La instrucción "js" salta cuando el bit de signo de EFLAGS es 1
- La instrucción jmp salta incondicionalmente la etiqueta "bucle"

¿Qué longitud de vector puede albergar un registro xmm?
1
- Depende del tipo de dato de la instrucción
- 8
- 4

¿Cual de las siguientes afirmaciones es falsa?
3
- Un informe SPEC proporciona una descripción detallada del Hardware, Software y parámetros de compilación
- Un informe SPEC publica resultadas de tiempos de ejecución básicas y optimizados
- Un informe SPEC publica resultados mediante tiempos de ejecución pero nunca ratios o eceleraciones

```_declspec(align(16)) float valeur1[4];  \n_declspec(align(16)) float valeur2[4]; \n_declspec(align(16)) float valeur3[4]; \n_declspec(align(16)) float valeur4[4]; \n \n// load vector 4x1 values in XMM0 to XMM3 \nmovaps xmm0, valeur1 \nmovaps xmm1, valeur2 \nmovaps xmm2, valeur3 \nmovaps xmm3, valeur4 \n \n// perform multiplication between matrix values and vector values \nmulps xmm0, xmm4 \nmulps xmm1, xmm5 \nmulps xmm2, xmm6 \nmulps xmm3, xmm7 \n``` \nEste fragmento de código dei ejemplo de multiplicación de matrices de la práctica 2 utiliza instrucción "mulps" para reaiizar lo siguiente:
2
- Multiplica dos operandos con datos empaquetados en punto fotante de doble precisión
- Multiplica dos operandos con datos empaquetados en punto fotante de simple precisión
- Multiplica dos operandos con datos empaquetados enteros

Ante la instrucción: ``` mov eax, [vector+2*edi] ``` ¿Cual de las siguientes afirmaciones es correcta en referencia al 2?
3
- Iteramos sobre el vector de 2 en 2 posiciones
- Es el tamaño del vector, qué solo tiene 2 elementos
- Es el tamaño en bytes del tipo de datos de vector
